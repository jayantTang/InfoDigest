# Test Environment
NODE_ENV=test
PORT=3001

# Database (test)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=infodigest_test
DB_USER=postgres
DB_PASSWORD=postgres

# Redis (test - optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# API Keys (use test keys or mocks)
NEWS_API_KEY=test_key
STOCK_API_KEY=

# LLM Configuration (use mock in tests)
LLM_PROVIDER=mock
LLM_MODEL=mock-model
DEEPSEEK_API_KEY=

# APNs (disabled in tests)
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_BUNDLE_ID=com.test.InfoDigest
APNS_KEY_PATH=./certs/test.p8
APNS_PRODUCTION=false

# Cron Schedule (disabled in tests)
CRON_SCHEDULE=

# Logging
LOG_LEVEL=error
